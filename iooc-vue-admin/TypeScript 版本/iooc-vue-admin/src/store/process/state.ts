
import {ProcessStateTypes} from "@/store/interface"

export const state: ProcessStateTypes = {
    list: [
        {
            label: "首页",
            value: "/",
            active: true
        }
    ]
}
